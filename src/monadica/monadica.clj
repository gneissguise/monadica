(ns monadica.monadica
  (:gen-class))

;; Outlining goal of monadica
;; - lightweight clojure webhosting
;; - continuous integration
;; - live repl/testing
;; - devops / orchestration via clojure
;; - when I say clojure I really mean babashka
;; - emacs plugin
;; - ring / malli / reitit / clojure library glue defaults
;; - xtdb - datalog - graph db
;; - business rules
;; - domain design schema
;; - component/donut style componentization
;; - tree-sitter / zipper / fast hiccup patching
;; - static main site / blog options
;; - and many more on channel 4

(defn -main
  [& args]
  (println "Blorp."))
